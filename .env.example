# --- UkisAI (OpenAI-compatible chat / agent LLM) ---
# Base URL including /v1 if your provider expects it.
UKISAI_API_BASE=https://api.ukisai.academy/v1
# API secret; use dummy if your endpoint does not require a key.
UKISAI_API_KEY=dummy
# Optional: pin a model id to skip GET /models discovery.
UKISAI_MODEL=

# --- Tavily (live web search for prices + assembly guide snippets) ---
TAVILY_API_KEY=
